import React, { FC } from 'react';
// styles
import { CharacterListContainer } from './CharacterList.styled';

const CharacterList: FC = () => {
  return <CharacterListContainer>Character list</CharacterListContainer>;
};

export default CharacterList;
